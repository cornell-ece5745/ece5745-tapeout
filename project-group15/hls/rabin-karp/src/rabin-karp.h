#include <ac_channel.h>

void rabinKarp(ac_channel<int > &pattern, ac_channel<int > &text, 
                ac_channel<int > &q, ac_channel<int > &text_size, 
                ac_channel<int > &pattern_size, ac_channel<int > &out);
